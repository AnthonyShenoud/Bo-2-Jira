name: Sync Jira Issues to GitHub

on:
  workflow_dispatch:  # Manual trigger for now

jobs:
  sync-jira:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Fetch Jira Issues
        env:
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_DOMAIN: "anthonyashenouda.atlassian.net"
          JIRA_PROJECT_KEY: "SCRUM"
          JIRA_USER: "anthony.ashenouda@gmail.com"
        run: |
          echo "Fetching Jira issues..."
          response=$(curl -s -X GET -u "$JIRA_USER:$JIRA_API_TOKEN" \
            -H "Accept: application/json" \
            "https://$JIRA_DOMAIN/rest/api/2/search?jql=project=$JIRA_PROJECT_KEY")
          
          echo "Raw response: $response"

          echo "$response" | jq '[.issues[] | {
            id: .id,
            key: .key,
            title: .fields.summary,
            body: .fields.description,
            due_date: .fields.duedate,
            input: .fields.customfield_10039,
            output: .fields.customfield_10040,
            priority: .fields.priority.name,
            team: (.fields.team.name // "No Team"),
            status: .fields.status.name,
            assignee: (.fields.assignee.emailAddress // empty)
          }]' > jira_issues.json

      - name: Debug Jira JSON Content
        run: |
          echo "Parsed Jira issues:"
          cat jira_issues.json

      - name: Create GitHub Issues
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PERSONAL_ACCESS_TOKEN }}
          REPO: "AnthonyShenoud/Bo-2-Jira"
          PROJECT_ID: "PVT_kwHOBF3sMs4A1ZhT"
          STATUS_FIELD_ID: "PVTSSF_lAHOBF3sMs4A1ZhTzgq4Do0"
        run: |
          declare -A STATUS_MAP=(
            ["To Do"]="61e4505c"
            ["In Progress"]="47fc9ee4"
            ["In Review"]="df73e18b"
            ["Backlog"]="f75ad846"
            ["Done"]="98236657"
          )

          declare -A USER_MAP=(
            ["anthony.ashenouda@gmail.com"]="AnthonyShenoud"
            ["anthony.ashenouda1@gmail.com"]="TintinShenouda"
            ["mariacampagnolo2001@gmail.com"]="MariaCampagnolo"
          )

          jq -c '.[]' jira_issues.json | while read -r issue; do
            title=$(echo "$issue" | jq -r '.title')
            body=$(echo "$issue" | jq -r '.body')
            jira_key=$(echo "$issue" | jq -r '.key')
            due_date=$(echo "$issue" | jq -r '.due_date')
            input=$(echo "$issue" | jq -r '.input')
            output=$(echo "$issue" | jq -r '.output')
            team=$(echo "$issue" | jq -r '.team')
            priority=$(echo "$issue" | jq -r '.priority')
            status=$(echo "$issue" | jq -r '.status')
            assignee=$(echo "$issue" | jq -r '.assignee')

            github_username="${USER_MAP[$assignee]}"
            assignees_json="[]"
            if [[ -n "$github_username" ]]; then
              assignees_json="[\"$github_username\"]"
            fi

            echo "Creating issue for: $jira_key ($title)"

            issue_response=$(curl -s -X POST \
              -H "Authorization: token $GITHUB_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              https://api.github.com/repos/$REPO/issues \
              -d "{
                \"title\": \"$title\",
                \"body\": \"### Jira: $jira_key\\n\\n**Description**: $body\\n\\n**Due**: $due_date\\n**Input**: $input\\n**Output**: $output\\n**Team**: $team\\n**Priority**: $priority\",
                \"assignees\": $assignees_json
              }")

            echo "GitHub API response: $issue_response"

            issue_node_id=$(echo "$issue_response" | jq -r '.node_id')
            issue_url=$(echo "$issue_response" | jq -r '.html_url')

            if [[ "$issue_node_id" == "null" || -z "$issue_node_id" ]]; then
              echo "⚠️ Failed to create GitHub issue for $jira_key"
              continue
            fi

            echo "✅ Created issue: $issue_url"

            # Add to GitHub Project
            item_id=$(gh api graphql -f query='
              mutation($projectId:ID!, $contentId:ID!) {
                addProjectV2ItemById(input: {projectId: $projectId, contentId: $contentId}) {
                  item { id }
                }
              }' -F projectId="$PROJECT_ID" -F contentId="$issue_node_id" --jq '.data.addProjectV2ItemById.item.id')
            
            echo "📦 Added to project. Item ID: $item_id"
            
            # Update status
            status_option_id="${STATUS_MAP[$status]}"
            if [[ -z "$status_option_id" ]]; then
              echo "⚠️ No valid status mapping for '$status'. Skipping status update."
              continue
            fi
            
            if [[ -z "$PROJECT_ID" || -z "$item_id" || -z "$STATUS_FIELD_ID" ]]; then
              echo "⚠️ Missing required variables for status update. Skipping."
              continue
            fi
            
            echo "PROJECT_ID: $PROJECT_ID"
            echo "item_id: $item_id"
            echo "STATUS_FIELD_ID: $STATUS_FIELD_ID"
            echo "status_option_id: $status_option_id"
            
            # Debug JSON construction
            json_input=$(jq -c --arg projectId "$PROJECT_ID" \
                              --arg itemId "$item_id" \
                              --arg fieldId "$STATUS_FIELD_ID" \
                              --arg optionId "$status_option_id" \
                              '{
                                projectId: $projectId,
                                itemId: $itemId,
                                fieldId: $fieldId,
                                value: { singleSelectOptionId: $optionId }
                              }' 2>&1)
            
            if [[ -z "$json_input" || "$json_input" == *"error"* ]]; then
              echo "⚠️ JSON construction failed. jq output: $json_input"
              continue
            fi
            
            echo "JSON Input for Status Update: $json_input"
            
            # Attempt to update status
            gh api graphql -F query='
              mutation UpdateStatus($input: UpdateProjectV2ItemFieldValueInput!) {
                updateProjectV2ItemFieldValue(input: $input) {
                  projectV2Item { id }
                }
              }' -F input="$json_input" || {
                echo "⚠️ Failed to update status for item $item_id. Skipping."
                continue
              }
            
            echo "✅ Status set to $status"
          done
